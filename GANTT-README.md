# チームスケジュール ガントチャート（gantt.html）

Windows PC のブラウザで動く、単一 HTML ファイルのガントチャートアプリです。
インストール不要で、`gantt.html` をダブルクリックするだけで使えます。

> **サーバーもインターネットも不要です。**
> `gantt.html` 1ファイルで完結しており、外部との通信は一切ありません
> （ファイル内に外部URL・通信コードはゼロ）。GitHub は開発時の置き場所として使っているだけで、
> アプリの動作には関係ありません。オフラインのPCでもそのまま動きます。

- **縦軸**：メンバー
- **横軸**：時間軸（**日**表示＝1時間単位／**週**表示＝1日単位／**月**表示＝1週間単位）
- 各作業の **担当者・作業内容・開始時刻・終了時刻** を登録
- **複数名が関わる作業は、1件の作業に担当者をまとめてアサイン**
- 時間軸は **I TIME（PCの時刻）の下に Z TIME（UTC）を小さく併記**
- **週表示では、チャートの下にその日の作業を文字でも一覧表示**
- 同じ時間に重なる作業は自動で段組み表示
- 現在時刻を赤い縦線で表示、土日は色分け
- **共有フォルダ経由で、約5秒ごとに全員とリアルタイム同期**
- **年1回更新の資格を期限管理し、属人化・期限集中を警告**
- **長期出張・訓練・来訪などの全体予定を、全表示の最上部と背景帯で把握**

## 基本操作

| 操作 | やり方 |
| --- | --- |
| 表示切替 | 上部の「日 / 週 / 月」ボタン |
| 期間の移動 | 「◀ / ▶」、現在に戻るには「今日」 |
| 作業の追加 | 「＋ 新規作業」、または表の**空いている場所をクリック**（その担当者・その時間帯で新規作成） |
| 複数名のアサイン | 入力画面の担当者欄で**複数チェック**（「全員」ボタンで一括選択） |
| 作業の編集・削除 | **バーをクリック** |
| メンバーの追加・改名・並び替え・削除 | 「メンバー」ボタン |
| 同期フォルダの指定・このPCで入力する人 | 「**⚙ 設定**」ボタン |
| 資格の期限管理 | 「**🎓 資格**」ボタン（元に戻すには「🗓 スケジュール」） |
| 全体予定の追加 | 「**＋ 全体予定**」ボタン、または全体予定レーンをクリック |
| ダイアログを閉じる | `Esc` |

作業には色とメモも付けられます。バーにマウスを乗せると、日時・担当者・メモが吹き出しで出ます。

## 全体予定（長期出張・訓練・来訪など）

個人の作業とは別に、**チーム全体で把握したい予定**を登録できます。
月表示で「今後何があるか」を見通すための機能です。

### 登録

「**＋ 全体予定**」ボタン、またはチャート最上部の**全体予定レーンをクリック**します。

| 項目 | 内容 |
|---|---|
| 内容 | 例：北海道出張／新人訓練／本部長来訪 |
| 種別 | **出張／訓練／来訪／その他**（色分けされます） |
| 期間 | 日単位（終了日を含む） |
| 対象メンバー | 任意。選ばなければ「全員向け」として扱います |

### 表示

- **全体予定レーン** — 日・週・月**すべての表示**で最上部に固定表示。重なる予定は自動で段組み
- **背景帯** — 登録した期間が各メンバー行の背景に薄く色づき、
  「この期間は誰が抜けるのか」を個人の予定と重ねて確認できます
- **今後の全体予定** — **月表示**のときチャートの下に一覧表示（今後4ヶ月ぶん）。
  期間・日数・開始までの残り日数・対象者が並び、クリックで編集できます

1日だけの来訪などは、バーが細くなっても内容が読めるよう表示を調整しています。

## 資格の期限管理（🎓 資格）

年1回更新の資格を、メンバー×資格種類のマトリクスで管理します。
「🎓 資格」ボタンで画面を切り替えます。

### 登録のしかた

1. 「**＋ 資格の種類**」で管理したい資格を追加（フォークリフト、玉掛け など。何種類でも可）
2. セルをクリックして期限を登録

期限の入力は**2通り**用意しています。

| 方法 | 使う場面 |
|---|---|
| **有効期限を直接入力** | 初回登録、期限がずれた場合の修正 |
| **「更新した（期限を1年延長）」ボタン** | 通常の年次更新。押すだけで1年後になる |

「更新した」を押すと、押した結果の日付が事前に表示されます。
期限切れ後に押した場合は**今日から1年後**になります。更新した日も記録されます。

### 色分け

| 色 | 状態 |
|---|---|
| 緑 | 有効（61日以上） |
| 黄 | 60日以内 |
| 橙 | 30日以内 |
| 赤 | 期限切れ |
| グレー | 未保有 |

### 各自が自分の期限を認識する

画面上部に**お知らせバナー**が出ます。「このPCで入力する人」を設定していれば、
**自分の期限が先頭**に表示されます（例：「⚠ あなたの 高所作業車 は残り5日です」）。
資格画面では自分の行が青帯で強調されます。

### 相互補完のための警告

単に期限を並べるだけでなく、**カバーできなくなる状態を先に警告**します。

- **属人化の警告** — 有効な保有者が1名以下の資格。その人が期限切れ・不在になると誰も対応できません
- **期限集中の警告** — 同じ月に複数名の期限が重なり、その月を過ぎると保有者が1名以下になる場合。
  年1回更新は放っておくと時期が固まりがちなので、更新をずらす判断に使えます
- **今日動ける人** — 資格名をクリックすると、今日その資格で有効な人が一覧で出ます

各資格の列見出しには「有効な保有者 ◯名」が常に表示され、1名以下なら赤くなります。

資格データもスケジュールと同じ仕組みで**全員に自動同期**されます（別途の設定は不要）。

## 共有フォルダでリアルタイムに同期する（推奨）

全員が同じ共有フォルダ（ファイルサーバー、NAS、クラウド同期フォルダなど）を見られる場合は、
**約5秒ごとに自動同期**します。サーバーの構築もインターネットも不要です。

### 準備

**全員が「⚙ 設定」→「同期フォルダを選ぶ…」で同じ共有フォルダを指定するだけ**です。

フォルダの中に同期ファイル（`team-schedule.json`）が無ければ自動で作られるので、
誰が最初に設定しても構いません。設定画面に「✓ 同期中」とフォルダ名が表示されれば完了です。

以降は入力するだけで自動保存され、他の人の変更も自動で入ってきます。
画面右上に緑のランプと「`team-schedule.json`：保存済み」と出ていれば同期中です。

用途ごとにボードを分けたい場合は「同期ファイル名」を変更できます
（全員で同じ名前にしてください）。

**2回目以降は自動で再接続**します。ブラウザの仕様で許可が切れている場合だけ、
「⚙ 設定」を開いてボタンを1回押し直してください。
同期をやめるときは「⚙ 設定」→「同期をやめる」（データはPCに残ります）。

> フォルダの指定が使えないブラウザでは、設定画面の
> 「フォルダを選べない場合はこちら」から、ファイルを直接指定できます。

### 同時に編集しても消えない仕組み

複数のPCが同じファイルを書くため、そのままでは**後から保存した人が先の人の入力を消してしまいます**。
これを防ぐため、次の動作を入れています。

1. 保存する前に、必ず共有ファイルを読み直してからマージして書き込む
2. 読み込んだ結果、**手元にしか無いデータがあれば自動的に書き戻す**
3. 各PCで保存タイミングを少しずらし、書き込みが重なりにくくする

実際に3台のPCが同時に5件ずつ立て続けに入力するテストを行い、
**15件すべてが約3秒で全員の画面に揃う**ことを確認しています。

同じ作業を2人が同時に編集したときだけ、後から保存した内容が残ります。

### 注意点

- **Microsoft Edge / Google Chrome** が必要です（この同期機能はブラウザのファイルアクセス機能を使うため）
- 共有フォルダが一時的に切れると「共有フォルダに接続できません」と表示され、復旧すると自動で戻ります
- うまく動かない場合は、下記の「JSONを書き出す／読み込む」でのやり取りに切り替えられます

## 各自のPCで入力して、メールでやり取りする

共有フォルダが使えない相手（別拠点など）とは、**各PCで入力 → JSONをメールで交換**という運用ができます。
サーバーもインターネット接続も不要です。

### 準備（1回だけ）

1. `gantt.html` を全員に配る（メール添付、USBメモリ、共有フォルダ、どれでも可）
2. 各自が任意のフォルダに保存し、ダブルクリックで開く
3. 各自が「⚙ 設定」画面で **「このPCで入力する人」** に自分を選ぶ

「このPCで入力する人」を設定すると、新しい作業の担当者が最初から自分になり、
一覧でも自分の行に「あなた」と表示されて見分けやすくなります。
この設定は**そのPCだけのもの**で、書き出すJSONには含まれません。

### 毎回の流れ

| 手順 | 操作 |
|---|---|
| 1. 各自が入力 | 自分のPCで自分の予定を入力 |
| 2. 各自が書き出し | 「?」→「JSONを書き出す」→ メールに添付して取りまとめ役へ |
| 3. 取りまとめ役が集約 | 届いたJSONを1つずつ「JSONを読み込む」で取り込む |
| 4. 全員に返信 | まとまったJSONを「JSONを書き出す」して全員に返信 |
| 5. 各自が読み込み | 受け取ったJSONを「JSONを読み込む」→ 全員の画面が揃う |

書き出したファイル名には**日付と名前が自動で付く**ので
（例：`team-schedule_20260728_山田太郎.json`）、誰の分か一目で分かります。

読み込むたびに「**追加3件 / 更新1件 / 削除1件**」と表示されるので、反映されたか確認できます。

### 上書きされない仕組み

読み込みは**上書きではなくマージ**です。

- 作業ごとに**更新時刻の新しい方**が残ります
- 別々の人が別々の予定を入力していれば、**両方とも残ります**
- 誰かが削除した予定は、他のPCでも**きちんと消えます**
- 各PCで追加したメンバーも合流します
- 同じ作業を2人が同時に直したときだけ、**後から編集した方**が残ります（担当を分けておけば衝突しません）

> **注意**
> - 削除の記録は**30日間**保持されます。それより古いJSONを読み込ませると、削除したはずの予定が復活することがあります。やり取りは30日以内に行ってください。
> - 更新時刻は各PCの時計を使うため、**PCの時刻がずれていると新旧の判定もずれます**。時刻は合わせておいてください。
> - 会社のメールフィルタが `.json` や `.html` の添付を止めることがあります。その場合はZIPにまとめて送ってください。

## Z TIME（UTC）の併記

時間軸の目盛りは、**上段が I TIME（このPCのローカル時刻）**、**下段に小さく Z TIME（UTC）** を表示します。
表示形式は、日表示が `時時分分Z`（例 `0000Z`）、週・月表示が `日日時時分分Z`（例 `261500Z`）です。

作業の入力画面でも、開始・終了それぞれの入力欄の下に `Z TIME: 271200Z` の形式で表示され、
時刻を変更すると即座に追従します。週表示の作業一覧にも Z TIME を併記します。

> 上段の見出し文字（`I` の部分）は、PCのタイムゾーン設定から自動判定します
> （UTC+9 なら `I`、UTC なら `Z`）。

## 週表示の作業一覧

週表示のときは、チャートの下に **その週の作業が日ごとの文字一覧** で並びます。
バーが細くて読めない場合でも、作業内容・時刻・担当者を文字で確認できます。

- 1日1列（計7列）で、各作業を **時刻／作業内容／担当者名** の順に表示
- 日をまたぐ作業には `←` `→` が付きます
- **日付をクリック**するとその日の日表示に切り替わります
- **作業をクリック**すると編集画面が開きます
- 上部の「作業一覧を隠す／表示」で開閉でき、状態は次回起動時も保たれます

## 複数人が関わる作業

作業の入力画面で **担当者をチェックボックスで複数選択** できます（「全員」で一括選択、「解除」で全解除）。

- 複数人をアサインしても **登録されるのは1件の作業** で、担当者全員の行に同じバーが表示されます
- バーには「3名」のように **人数バッジ** が付き、共同作業だと一目で分かります
- どの行のバーを開いても **同じ1件を編集** します。時間や内容の変更は全員に反映され、削除すると全員の予定から消えます
- 特定の人だけ外したいときは、作業を開いて **その人のチェックを外す** だけです

## データの保管について

- 入力内容は**そのPCのブラウザ（localStorage）にも常に自動保存**されるため、共有ファイルに未接続でもデータは残ります。
- バックアップは「JSONを書き出す」で取得できます。
- 削除した作業は同期のため 30 日間だけ「削除済み」の印として残り、その後自動的に消えます。

## 動作確認済みの環境

Chromium 系ブラウザ（Microsoft Edge / Google Chrome）。
自動同期以外の機能は Firefox でも動作します。
